# find-the-intruder
Simple program that find an intruder in a number array

The program search in a small array of numbers a number: x. If all number in the array are equal to x return 1, else return 0.
